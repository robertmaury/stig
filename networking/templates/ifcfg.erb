DEVICE="<%= @localinterface %>"
BOOTPROTO="static"
HOSTNAME="<%= @fqdn %>"
GATEWAY="<%= scope.lookupvar('stig::gateway') %>"
HWADDR="<%= @macaddress %>"
IPADDR="<%= @ipaddress %>"
NETMASK="<%= @netmask %>"
NM_CONTROLLED="no"
ONBOOT="yes"
